{
  "dependencies": {
    "axios": "^1.6.0",
    "bootstrap": "^5.3.2",
    "server": "json-server --watch src/Data/mazes.json --port 5000"
  },
  "devDependencies": {
    "json-server": "^0.17.4",
    "live-server": "^1.2.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "json-server": "json-server --watch ui/src/Data/mazes.json --port 5000"
  }
}
